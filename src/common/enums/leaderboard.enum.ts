export enum Rank {
    BRONZE = "bronze",
    SILVER = "silver",
    GOLD = "gold",
    MASTER = "master",
    EPIC = "epic",
    LEGEND = "legend"
}

export enum LeaderboardType {
    REFERRAL = 1,
    COIN = 2,
    QUEST = 3
}

export enum LeaderboardPeriod {
    DAILY = 1,
    WEEKLY = 2,
    MONTHLY = 3,
    ALLTIME = 4
}

export enum LeaderboardSeasonStatus {
    OPEN = 1,
    END = 2,
    CANCELED = 3
}
